name: "ANN_Heston_IV"

type: "mlp"

input:
  name: "heston_params_m_tau_r"
  dim: 8

hidden:
  dims: [200, 200, 200, 200]
  activation: "relu"
  initialization: "xavier_uniform"  # equivalent to Golub
  dropout_rate: 0.0
  batch_norm: false

output:
  dim: 1
  name: "IV"
  activation: "linear"
