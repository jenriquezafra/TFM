\chapter{Neural Network Performance And Benchmarking}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Experimental Setup And Evaluation Protocol}
This chapter compares the three model blocks developed in the thesis under a common evaluation protocol: ANN pricing surrogate, calibration network workflow, and PINN-based pricing and Greeks computation.

Unless explicitly stated, all reported metrics are computed on fixed evaluation sets with identical feature conventions, consistent preprocessing, and the same numerical references used in previous chapters.

The minimal pre-benchmark validation protocol combines three checks run before any model comparison: convergence with respect to \((N,L)\) in the COS expansion, put-call parity residual control across strikes, and Black--Scholes consistency in near-constant-variance regimes.

These checks are used as a guardrail before comparing models, so that downstream differences can be attributed to model behavior rather than to numerical artifacts. For consistency across Sections~6.2--6.5, each block is analyzed with the same reading logic: global summary metrics, localized error diagnostics over relevant domains, and runtime indicators.

The protocol is fixed as follows. Randomness is controlled with a common seed (\(42\)) in data generation and training configurations; in calibration, the Differential Evolution seed follows the same base value unless explicitly overridden. The Heston domain is shared across experiments, with \(\rho\in[-0.9,0]\), \(\kappa\in[0,3]\), \(\gamma\in[0.01,0.8]\), \(\bar v\in[0.01,0.5]\), \(v_0\in[0.05,0.5]\), and contract coordinates \(m\in[0.6,1.4]\), \(\tau\in[0.05,3.0]\) for ANN/CaNN benchmarks. Reference numerics are also fixed: COS pricing with \(N=1500\), \(L=50\), and fallback \(L=80\) when required by stability checks, plus Brent IV inversion with \(\mathrm{tol}=10^{-8}\) and bounded search interval.

Reported metrics are always computed on fixed held-out sets, with ANN split policy fixed to \(0.8/0.1/0.1\) (train/validation/test), and values reported in decimal units (IV for ANN/CaNN, price for PINN). Finally, each model block reports equivalent evidence layers (global table, regional/spatial diagnostic, and convergence or runtime indicator), so cross-model comparisons remain methodologically aligned.

Methodological details of each block are kept in their dedicated chapters (Chapter~3 for ANN, Chapter~4 for CaNN, and Chapter~5 for PINN), while this chapter focuses on consolidated benchmark outcomes under the protocol above.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{ANN Pricer Performance}
This section reports the performance of the supervised ANN pricer introduced in Chapter~3. The objective is to assess mean implied-volatility accuracy and spatial stability over \((m,\tau)\), using a fully specified evaluation protocol that can be reproduced without implementation-specific references.

The ANN analyzed here corresponds to the selected baseline configuration from Chapter~3. The benchmark uses fixed weights (no retraining in Chapter~6) and fixed train/validation/test splits of sizes \(800{,}000/100{,}000/100{,}000\). Pointwise error is defined as
\[
e=\hat{\sigma}_{\mathrm{IV}}-\sigma_{\mathrm{IV}}^{\mathrm{ref}},
\]
reported in decimal implied-volatility units.

Global quality is summarized with RMSE, MAE, and \(p_{90}(|e|)\). Regional diagnostics use fixed bins over \(m\) and \(\tau\), and extrapolation behavior is analyzed through sensitivity maps with explicit interpolation limits (Figure~\ref{fig:extrapolation_grid_ann}).

\subsection{Global Error Metrics}

Table~\ref{tab:ann_global_metrics} reports split-wise global errors for the selected ANN baseline.

\begin{table}[H]
    \ttabbox[\FBwidth]
    {\caption{Global ANN implied-volatility errors (baseline configuration).}\label{tab:ann_global_metrics}}
    {\begin{tabular}{|P{1.8cm}|P{1.8cm}|P{2.2cm}|P{2.2cm}|P{2.2cm}|}
        \hline
        \textbf{Split} & \(\mathbf{n}\) & \textbf{RMSE} & \textbf{MAE} & \(\mathbf{p_{90}(|e|)}\) \\
        \hline
        Train & 800,000 & \(5.44\times10^{-4}\) & \(3.82\times10^{-4}\) & \(8.15\times10^{-4}\) \\
        \hline
        Validation & 100,000 & \(2.16\times10^{-3}\) & \(4.11\times10^{-4}\) & \(8.27\times10^{-4}\) \\
        \hline
        Test & 100,000 & \(3.03\times10^{-3}\) & \(4.29\times10^{-4}\) & \(8.28\times10^{-4}\) \\
        \hline
        \multicolumn{5}{l}{Source: Own elaboration}
    \end{tabular}}
\end{table}

The test errors remain small in absolute IV units, and the upper-quantile behavior (\(p_{90}\)) stays in the \(10^{-3}\) range, which is consistent with a stable surrogate in the interpolation region.

Figure~\ref{fig:extrapolation_grid_ann} provides a panel-wise diagnostic. Each row corresponds to one parameter plane: top \((\gamma,\bar v)\) (vol-of-vol versus long-run variance), middle \((\rho,\gamma)\) (correlation versus vol-of-vol), and bottom \((\kappa,\bar v)\) (mean reversion versus long-run variance). The left column shows ANN IV maps with white isoclines, while the right column shows \(\log |\,\mathrm{NN}-\mathrm{Heston}\,|\) on the same grids. The dashed black lines delimit interpolation limits; regions beyond those limits are extrapolation zones where the network was not trained.

The most stable behavior is observed in the interior of each plane, where isoclines are smooth and error colors remain comparatively mild. Error increases near boundary segments and especially outside the dashed limits, with the strongest deterioration concentrated in outer corners of the tested planes. This is consistent with the regional-error reading: the surrogate remains accurate over the interpolation core, while low-moneyness/short-maturity-like boundary regimes are the most fragile. In practical terms, Figure~\ref{fig:extrapolation_grid_ann} supports using the ANN as a robust interpolation surrogate and treating extrapolated regions as lower-confidence zones.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{figures/nn/sensitivity_grid_3x2_log.png}
    \caption{Sensitivity grid for the ANN pricer. Top row: \((\gamma,\bar v)\), middle row: \((\rho,\gamma)\), bottom row: \((\kappa,\bar v)\). Left panels report IV-value maps with white isoclines; right panels report \(\log|\,\mathrm{NN}-\mathrm{Heston}\,|\). Dashed black lines indicate interpolation limits.}
    \label{fig:extrapolation_grid_ann}
\end{figure}

\todos{MEJORAR ESTA FIGURA}

\subsection{Regional Error Profile}

Regional diagnostics identify where approximation error concentrates. Table~\ref{tab:ann_regional_profile} reports representative bins from the test split; \textit{Share (\%)} indicates the fraction of test points that fall into each bin, so error magnitude can be interpreted jointly with sample coverage.

\begin{table}[H]
    \ttabbox[\FBwidth]
    {\caption{Representative regional ANN errors on the test split.}\label{tab:ann_regional_profile}}
    {\begin{tabular}{|P{2.2cm}|P{2.2cm}|P{1.8cm}|P{1.8cm}|P{1.8cm}|}
        \hline
        \textbf{Feature} & \textbf{Bin} & \textbf{Share (\%)} & \textbf{RMSE} & \textbf{MAE} \\
        \hline
        \(m\) & \([0.6,0.8]\) & 25.11 & \(5.98\times10^{-3}\) & \(6.11\times10^{-4}\) \\
        \hline
        \(\tau\) & \([0.05,0.25]\) & 6.79 & \(1.15\times10^{-2}\) & \(1.23\times10^{-3}\) \\
        \hline
        \(m\) & \([1.1,1.2]\) & 12.30 & \(4.73\times10^{-4}\) & \(3.51\times10^{-4}\) \\
        \hline
        \(\tau\) & \([2.0,3.0]\) & 33.89 & \(4.45\times10^{-4}\) & \(3.23\times10^{-4}\) \\
        \hline
        \multicolumn{5}{l}{Source: Own elaboration}
    \end{tabular}}
\end{table}

The reading is the following. The high-error bins are concentrated at low moneyness and short maturity (\([0.6,0.8]\) in \(m\), \([0.05,0.25]\) in \(\tau\)). The two lower-error rows represent interior/longer-horizon regions and show a clear error drop. This pattern is therefore structural, not a rare-point artifact: the most difficult moneyness bin still carries a large share of the test set (25.11\%).

Figure~\ref{fig:region_error_mse_ann} makes the same pattern explicit with two panels: left panel (MSE by moneyness bins) and right panel (MSE by maturity bins). The figure reports only bin-level MSE curves (train/validation/test); it does not display sample coverage. Coverage is exactly what the \textit{Share (\%)} column in Table~\ref{tab:ann_regional_profile} represents. In both validation and test curves, the first bin is clearly the most difficult, while errors drop sharply and remain much lower in interior bins. This is consistent with boundary/extrapolation effects and with the smoother behavior observed around central bins.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{figures/nn/eval_region_mse.png}
    \caption{Regional ANN MSE by moneyness bins (left) and maturity bins (right). Curves correspond to train, validation, and test.}
    \label{fig:region_error_mse_ann}
\end{figure}

\todos{MEJORAR ESTA FIGURA}


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Calibration Network Performance}
This section reports the benchmark outcomes of the CaNN workflow. The full calibration methodology (problem formulation, optimizer mechanics, and detailed design choices) is developed in Chapter~4; here we focus on final metrics and on the selection of the reference calibration run used in the rest of the chapter.

\subsection{Benchmark Snapshot}

The calibration benchmark in this section uses a fixed ANN surrogate (from Section~6.2) and one fixed quote universe of 35 contracts (Table~\ref{tab:cann_quote_universe}). The calibrated vector is \((\rho,\kappa,\gamma,\bar v,v_0)\), optimized with Differential Evolution plus polish under the same bounds and controls defined in Chapter~4.

\begin{table}[H]
    \ttabbox[\FBwidth]
    {\caption{Quote universe used in the CaNN benchmark.}\label{tab:cann_quote_universe}}
    {\begin{tabular}{|P{4.2cm}|P{6.9cm}|}
        \hline
        \textbf{Item} & \textbf{Value} \\
        \hline
        Number of quotes & 35 \\
        \hline
        Grid structure & Complete \(5\times7\) grid over \((m,\tau)\) \\
        \hline
        Moneyness nodes \(m\) & \(\{0.85,0.925,1.00,1.075,1.15\}\) \\
        \hline
        Maturity nodes \(\tau\) & \(\{0.50,0.75,1.00,1.25,1.50,1.75,2.00\}\) \\
        \hline
        Interest rate \(r\) & 0.03 (constant across quotes) \\
        \hline
        \multicolumn{2}{l}{Source: Own elaboration}
    \end{tabular}}
\end{table}

\subsection{Fit Quality And Convergence}

A compact regularization mini-study was run with the same quote set and optimizer controls. The no-regularization variant (\(\lambda=0\)) yields residual RMSE \(2.01441\times10^{-4}\), while the L2 variant (\(\lambda=10^{-6}\)) yields \(2.01445\times10^{-4}\). The difference is marginal, but the no-regularization run is slightly better and is therefore selected as the reference configuration for the remainder of Section~6.3.

To characterize residual magnitudes directly in IV units, Table~\ref{tab:cann_residual_distribution} summarizes the absolute-residual distribution over the 35 quotes for the selected run.

\begin{table}[H]
    \ttabbox[\FBwidth]
    {\caption{Absolute-residual distribution across market quotes (selected run).}\label{tab:cann_residual_distribution}}
    {\begin{tabular}{|P{2.3cm}|P{2.3cm}|P{2.3cm}|P{2.3cm}|}
        \hline
        \textbf{Mean \(|r|\)} & \textbf{Median \(|r|\)} & \(\mathbf{p_{95}(|r|)}\) & \textbf{Max \(|r|\)} \\
        \hline
        \(1.70\times10^{-4}\) & \(1.58\times10^{-4}\) & \(3.30\times10^{-4}\) & \(4.52\times10^{-4}\) \\
        \hline
        \multicolumn{4}{l}{Source: Own elaboration}
    \end{tabular}}
\end{table}

Residual concentration is mild. The largest local deviations appear around intermediate-to-high moneyness and medium maturities, but their scale remains below \(5\times10^{-4}\) in absolute IV units.

Figure~\ref{fig:cann_residual_heatmap_m_tau} shows the signed residual map over \((m,\tau)\) for the reference no-regularization solution. The color pattern is balanced around zero (no strong systematic bias), with localized positive and negative pockets whose magnitude remains in the same \(10^{-4}\) scale reported in Table~\ref{tab:cann_residual_distribution}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth]{figures/cann/cann_residual_heatmap_m_tau_no_reg_smooth.png}
    \caption{Signed residual heatmap over \((m,\tau)\) for the CaNN reference calibration (no regularization). Residual defined as \(\mathrm{IV}_{\mathrm{pred}}-\mathrm{IV}_{\mathrm{market}}\). Rendering is visually smoothed for readability; values come from the original quote grid.}
    \label{fig:cann_residual_heatmap_m_tau}
\end{figure}

\todos{MEJORAR ESTA FIGURA}


\subsection{Parameter Recovery Profile}

Since this quote set includes sidecar synthetic truth, parameter-level errors can be reported for the reference no-regularization solution.

\begin{table}[H]
    \ttabbox[\FBwidth]
    {\caption{Parameter error profile for the reference CaNN calibration (no regularization).}\label{tab:cann_parameter_errors}}
    {\begin{tabular}{|P{2.0cm}|P{2.0cm}|P{2.0cm}|P{2.0cm}|}
        \hline
        \textbf{Parameter} & \textbf{True value} & \textbf{Abs. error} & \textbf{Rel. error} \\
        \hline
        \(\rho\) & \(-0.45\) & \(7.41\times10^{-2}\) & \(1.65\times10^{-1}\) \\
        \hline
        \(\kappa\) & \(1.00\) & \(5.76\times10^{-2}\) & \(5.76\times10^{-2}\) \\
        \hline
        \(\gamma\) & \(0.35\) & \(5.04\times10^{-2}\) & \(1.44\times10^{-1}\) \\
        \hline
        \(\bar v\) & \(0.20\) & \(2.42\times10^{-3}\) & \(1.21\times10^{-2}\) \\
        \hline
        \(v_0\) & \(0.22\) & \(2.89\times10^{-4}\) & \(1.31\times10^{-3}\) \\
        \hline
        \multicolumn{4}{l}{Source: Own elaboration}
    \end{tabular}}
\end{table}

The largest relative deviations appear in \(\rho\) and \(\gamma\), while \(\bar v\) and \(v_0\) are recovered with substantially smaller error.

\begin{table}[H]
    \ttabbox[\FBwidth]
    {\caption{Selected CaNN run versus Liu et al. (absolute parameter errors).}\label{tab:cann_vs_liu}}
    {\begin{tabular}{|P{3.1cm}|P{3.0cm}|P{3.0cm}|}
        \hline
        \textbf{Metric} & \textbf{This work (selected run)} & \textbf{Liu et al. \cite{liu}} \\
        \hline
        Weighted MSE & \(4.06\times10^{-8}\) & \(\sim 10^{-8}\) \\
        \hline
        \(|\rho-\rho^\star|\) & \(7.41\times10^{-2}\) & \(4.84\times10^{-2}\) \\
        \hline
        \(|\kappa-\kappa^\star|\) & \(5.76\times10^{-2}\) & \(4.88\times10^{-2}\) \\
        \hline
        \(|\gamma-\gamma^\star|\) & \(5.04\times10^{-2}\) & \(3.28\times10^{-2}\) \\
        \hline
        \(|\bar v-\bar v^\star|\) & \(2.42\times10^{-3}\) & \(4.54\times10^{-3}\) \\
        \hline
        \(|v_0-v_0^\star|\) & \(2.89\times10^{-4}\) & \(4.39\times10^{-4}\) \\
        \hline
        \multicolumn{3}{l}{Source: Own elaboration based on selected run outputs and \cite{liu}.}
    \end{tabular}}
\end{table}

In Table~\ref{tab:cann_vs_liu}, \textit{Weighted MSE} denotes the weighted mean-squared IV residual used as calibration objective (same weighting convention as Chapter~4). The pattern is consistent across both studies: \((\bar v,v_0)\) are recovered more accurately than \((\rho,\gamma)\). The comparison is informative but not a strict ranking, because quote set design, optimization budget, and implementation details are not identical.

\subsection{Local Smoothness And Curvature Study}

To characterize local identifiability around the calibrated optimum, Jacobian and Hessian diagnostics were computed at \(\theta^\star\) for the selected run.

\begin{table}[H]
    \ttabbox[\FBwidth]
    {\caption{Local-curvature diagnostics at the calibrated solution \(\theta^\star\) (selected run).}\label{tab:cann_hessian_diagnostics}}
    {\begin{tabular}{|P{2.6cm}|P{2.0cm}|P{2.0cm}|P{2.2cm}|}
        \hline
        \(\|\nabla J(\theta^\star)\|_2\) & \(\lambda_{\min}(H)\) & \(\lambda_{\max}(H)\) & \(\kappa_{\mathrm{abs}}(H)\) \\
        \hline
        \(6.87\times10^{-6}\) & \(5.15\times10^{-4}\) & \(4.16\times10^{1}\) & \(8.07\times10^{4}\) \\
        \hline
        \multicolumn{4}{l}{Source: Own elaboration}
    \end{tabular}}
\end{table}

The selected solution satisfies near-stationarity (\(\|\nabla J(\theta^\star)\|_2 \ll 10^{-4}\)). The Hessian is symmetric, and the reported extreme eigenvalues are both non-negative in this run, consistent with a locally convex basin around the selected optimum. At the same time, \(\kappa_{\mathrm{abs}}(H)\approx 8\times10^4\) indicates pronounced anisotropy (flat and stiff directions coexist). The strongest cross-parameter coupling appears between \(\bar v\) and \(v_0\) (off-diagonal entry \(\approx 18.69\)), followed by \((\gamma,v_0)\) and \((\gamma,\bar v)\), which is coherent with the observed recovery pattern.

Figure~\ref{fig:cann_jacobian_theta_star} shows the Jacobian components at \(\theta^\star\) for the no-regularization reference run. All components remain close to zero (between \(10^{-8}\) and \(10^{-6}\)), reinforcing first-order convergence. The largest entries are associated with \(\bar v\) and \(v_0\), but their magnitude is still very small, so there is no dominant residual slope left at the optimum.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth]{figures/cann/jacobian_theta_star_no_reg.png}
    \caption{Jacobian components at \(\theta^\star\) for the CaNN reference calibration (no regularization).}
    \label{fig:cann_jacobian_theta_star}
\end{figure}

\todos{MEJORAR ESTA FIGURA}


Figure~\ref{fig:cann_hessian_theta_star} is the key second-order diagnostic. The heatmap makes explicit a block structure centered on \((\bar v,v_0)\): both diagonal entries are the largest (high local curvature), and their off-diagonal term is also large, which indicates strong local coupling between long-run variance and initial variance. By contrast, the \(\kappa\)-related diagonal curvature is much smaller, so perturbations in that direction are less strongly penalized around \(\theta^\star\). This mixed pattern (stiff variance block plus flatter directions in other coordinates) explains why fit residuals can be very small while some parameters, especially \(\rho\) and \(\gamma\), remain harder to pin down with the same precision as \((\bar v,v_0)\).
\todos{Comentario visual: conservar esta figura como diagnóstico principal de curvatura local en CaNN.}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth]{figures/cann/hessian_theta_star_no_reg.png}
    \caption{Hessian matrix at \(\theta^\star\) for the CaNN reference calibration (no regularization).}
    \label{fig:cann_hessian_theta_star}
\end{figure}

\todos{MEJORAR ESTA FIGURA}

The results above define the selected CaNN benchmark used in the rest of this chapter.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{PINN Pricing Performance}
This section reports the baseline PINN pricing quality before introducing Sobolev terms in the training objective. The goal is to establish a reproducible reference for price-level accuracy and runtime against the COS/Heston engine.

\subsection{Reproducible Benchmark Setup}

The pricing benchmark compares a baseline parametric PINN against the COS/Heston reference under a common evaluation protocol and shared numerical conventions.

The evaluated model is the baseline parametric PINN (without Sobolev terms), benchmarked against the Heston-COS reference. Inputs follow the 8-dimensional convention
\[
\left[\tau, m, v, \rho, \kappa, \gamma, \bar v, r\right],
\]
with affine standardization fitted on training statistics. Concretely, each feature \(x_j\) is transformed as
\[
\tilde{x}_j=\frac{x_j-\mu_j^{\mathrm{train}}}{\sigma_j^{\mathrm{train}}},
\]
where \(\mu_j^{\mathrm{train}}\) and \(\sigma_j^{\mathrm{train}}\) are computed only on the training split and then reused unchanged in validation/test/benchmark evaluation. This is used to keep features on comparable scales and to stabilize training and gradient computation, which is especially important in PINN settings; it also avoids data-leakage from evaluation sets. The evaluation sample contains \(10{,}000\) candidates, with \(9{,}996\) valid comparisons after numerical filtering. COS pricing is computed with \(N=1500\), \(L=50\), and fallback \(L=80\), under European put convention (\(K=1\)) and no-arbitrage tolerance \(10^{-8}\). All reported metrics are computed in double precision on CPU.

\subsection{Global Pricing Metrics}

Table~\ref{tab:pinn_pricing_global_metrics} reports the compact global summary used in this chapter.

\begin{table}[H]
    \ttabbox[\FBwidth]
    {\caption{Global PINN pricing metrics versus COS reference.}\label{tab:pinn_pricing_global_metrics}}
    {\begin{tabular}{|P{5.1cm}|P{6.0cm}|}
        \hline
        \textbf{Metric} & \textbf{Value} \\
        \hline
        \(n\) evaluated points & 9,996 \\
        \hline
        MSE & \(3.1621 \times 10^{-5}\) \\
        \hline
        RMSE & \(5.6233 \times 10^{-3}\) \\
        \hline
        MAE & \(2.7458 \times 10^{-3}\) \\
        \hline
        \(p_{95}\) Abs. Err. & \(1.1840 \times 10^{-2}\) \\
        \hline
        MAPE (\%) & 16.0084 \\
        \hline
        \(R^2\) & 0.99957 \\
        \hline
        \multicolumn{2}{l}{Source: Own elaboration}
    \end{tabular}}
\end{table}

These results are obtained with the baseline PINN only (without Sobolev augmentation). In runtime terms, the PINN evaluates the full benchmark in \(0.0203\) s (\(\approx 4.91\times 10^5\) points/s), while COS requires \(114.47\) s (\(\approx 87.36\) points/s), i.e., an empirical throughput ratio of approximately \(5.63\times 10^3\).

\subsection{Spatial And Distributional Diagnostics}

The pointwise agreement between PINN and COS prices is first assessed in Figure~\ref{fig:pinn_pricing_scatter}. The cloud remains tightly concentrated around the identity line (\(R^2=0.99957\)), while a mild negative bias (mean error \(-1.87\times 10^{-3}\)) indicates slight underpricing in the central price range.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{figures/pinn/pricing/scatter_pinn_vs_cos.png}
    \caption{PINN price predictions versus COS reference values on the benchmark set.}
    \label{fig:pinn_pricing_scatter}
\end{figure}

The spatial structure of absolute pricing error is shown in Figure~\ref{fig:pinn_pricing_abs_error_map}. Error is not homogeneous over \((m,\tau)\): the largest deviations are concentrated in long maturities and moneyness tails, especially for \(m>1.2\) and \(\tau>2\), whereas the central region is markedly more stable.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{figures/pinn/pricing/abs_error_map_m_tau.png}
    \caption{Absolute pricing-error map over the \((m,\tau)\) benchmark grid.}
    \label{fig:pinn_pricing_abs_error_map}
\end{figure}

\todos{SUAVIZAR?}

Relative error patterns are presented in Figure~\ref{fig:pinn_pricing_rel_error_map}. These values must be interpreted jointly with absolute errors: when the COS reference price is close to zero, relative error inflates, so local high percentages do not necessarily correspond to large monetary mispricing.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{figures/pinn/pricing/rel_abs_error_map_m_tau.png}
    \caption{Relative absolute pricing-error map over the \((m,\tau)\) benchmark grid.}
    \label{fig:pinn_pricing_rel_error_map}
\end{figure}

\todos{SUAVIZAR?}


The global distribution of absolute errors is summarized in Figure~\ref{fig:pinn_pricing_abs_error_cdf}. The median absolute error is \(1.02\times10^{-3}\), \(p_{95}=1.18\times10^{-2}\), and only about \(1.7\%\) of points exceed \(2\times10^{-2}\), which confirms high overall accuracy with a limited but non-negligible tail.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{figures/pinn/pricing/abs_error_cdf.png}
    \caption{Empirical CDF of absolute pricing errors for the baseline PINN.}
    \label{fig:pinn_pricing_abs_error_cdf}
\end{figure}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{PINN Greeks Performance}
This section evaluates autodiff-based Greek quality for the baseline PINN and quantifies the effect of Sobolev augmentation through a controlled ablation.

\subsection{Ablation Design And Reproducible Protocol}
\label{sec:ablation_pinn_sobolev}
The ablation isolates the impact of Jacobian-informed Sobolev terms on top of the baseline PINN objective, comparing:
\begin{itemize}
    \item \textbf{Baseline PINN}: PDE + terminal + lower-boundary losses only.
    \item \textbf{Sobolev-augmented PINN}: baseline loss plus derivative supervision terms \(\mathcal{L}_{g,1}\) and \(\mathcal{L}_{g,2}\) as defined in Chapter~5.
\end{itemize}

To keep the comparison interpretable, both variants are benchmarked on the same \((m,\tau)\) grid, with identical fixed parameters, the same Heston characteristic-function Greek engine, and the same numerical integration settings.

\begin{table}[H]
    \ttabbox[\FBwidth]
    {\caption{Reproducible protocol for the Greek ablation benchmark.}\label{tab:pinn_greeks_ablation_setup}}
    {\begin{tabular}{|P{4.4cm}|P{6.7cm}|}
        \hline
        \textbf{Item} & \textbf{Value} \\
        \hline
        Compared variants & Baseline PINN vs Sobolev-augmented PINN \\
        \hline
        Architecture and features & Fixed across variants (same parametric PINN input convention) \\
        \hline
        Evaluation grid & \(m\in[0.8,1.2]\) (161 points), \(\tau\in[0.05,2.0]\) (161 points) \\
        \hline
        Total evaluation size & \(n=25{,}921\) points per Greek \\
        \hline
        Fixed model parameters & \(v=0.04,\ \rho=-0.7,\ \kappa=2.0,\ \gamma=0.3,\ \bar v=0.04,\ r=0.01\) \\
        \hline
        Reference Greeks & Semi-analytical Heston characteristic-function Greeks \\
        \hline
        Characteristic function integration setup & \(u_{\min}=10^{-6}\), \(u_{\max}=200\), \(n_u=1200\) \\
        \hline
        Shared conventions & Put option, strike \(K=1\), THETA sign convention (\(\mathrm{THETA}=-\partial V/\partial\tau\)), MAPE floor \(10^{-4}\) \\
        \hline
        Arithmetic context & Double precision on CPU \\
        \hline
        \multicolumn{2}{l}{Source: Own elaboration}
    \end{tabular}}
\end{table}

\subsection{Ablation Metrics By Greek}

Greek quality is reported with the metrics exported by the benchmark pipeline: RMSE, \(R^2\), MAPE (\%), and MAE.

\begin{table}[H]
    \ttabbox[\FBwidth]
    {\caption{Greek metrics for baseline and Sobolev PINN (\(n=25{,}921\) per Greek).}\label{tab:ablation_metrics_by_greek}}
    {\small
    \begin{tabular}{|P{1.8cm}|P{1.6cm}|P{1.6cm}|P{1.6cm}|P{1.6cm}|P{1.6cm}|}
        \hline
        \textbf{Variant} & \textbf{Greek} & \textbf{RMSE} & \(\mathbf{R^2}\) & \textbf{MAPE (\%)} & \textbf{MAE} \\
        \hline
        Baseline & DELTA & \(3.33\times 10^{-2}\) & 0.9831 & 10.74 & \(2.82\times 10^{-2}\) \\
        \hline
        Baseline & GAMMA & \(3.85\times 10^{-1}\) & 0.8592 & 82.98 & \(2.85\times 10^{-1}\) \\
        \hline
        Baseline & VEGA & \(6.55\times 10^{-2}\) & 0.5534 & 71.84 & \(5.80\times 10^{-2}\) \\
        \hline
        Baseline & THETA & \(4.82\times 10^{-3}\) & 0.9270 & 31.98 & \(3.10\times 10^{-3}\) \\
        \hline
        Baseline & RHO & \(8.37\times 10^{-2}\) & 0.9431 & 95.00 & \(7.16\times 10^{-2}\) \\
        \hline
        Sobolev & DELTA & \(3.51\times 10^{-4}\) & 1.0000 & 0.28 & \(2.41\times 10^{-4}\) \\
        \hline
        Sobolev & GAMMA & \(8.94\times 10^{-3}\) & 0.9999 & 31.29 & \(4.29\times 10^{-3}\) \\
        \hline
        Sobolev & VEGA & \(3.17\times 10^{-3}\) & 0.9990 & 36.11 & \(1.70\times 10^{-3}\) \\
        \hline
        Sobolev & THETA & \(5.30\times 10^{-4}\) & 0.9991 & 1.88 & \(1.27\times 10^{-4}\) \\
        \hline
        Sobolev & RHO & \(1.43\times 10^{-3}\) & 1.0000 & 9.05 & \(1.02\times 10^{-3}\) \\
        \hline
        \multicolumn{6}{l}{Source: Own elaboration}
    \end{tabular}}
\end{table}

Compared with the baseline PINN, Sobolev augmentation reduces RMSE by approximately \(9\times\) (THETA) to \(95\times\) (DELTA), with large \(R^2\) gains for the two hardest Greeks in the baseline model (GAMMA and VEGA). Greek-inference throughput decreases from \(9.48\times 10^3\) to \(8.60\times 10^3\) points/s (\(\sim 9.3\%\) reduction), which remains a clearly favorable trade-off given the global accuracy gains.

\subsection{Visual Diagnostics}

This subsection is organized as a before/after narrative. Baseline diagnostics are first used to localize the dominant Greek-error modes, and Sobolev diagnostics are then read with the same visual layout to make improvements directly comparable.

Figure~\ref{fig:no_sobolev_greeks_error_maps} displays baseline relative absolute-error maps for the five Greeks. The spatial pattern is heterogeneous, with broad high-error regions in GAMMA and VEGA, which is consistent with their weaker baseline \(R^2\) values in Table~\ref{tab:ablation_metrics_by_greek}.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{figures/pinn/greeks/no_sobolev_rel_abs_error_maps_5greeks.png}
    \caption{Relative absolute-error maps for baseline PINN Greeks (DELTA, GAMMA, VEGA, THETA, RHO).}
    \label{fig:no_sobolev_greeks_error_maps}
\end{figure}

\todos{MEJORAR ESTA FIGURA}

The baseline histogram panel complements this map-level view with error distributions. The heaviest tails appear in GAMMA and VEGA, while DELTA, THETA, and RHO are comparatively tighter. This confirms that the baseline model captures coarse sensitivity structure but still exhibits non-negligible extreme deviations in the hardest derivatives.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{figures/pinn/greeks/no_sobolev_error_hist_5greeks.png}
    \caption{Error histograms for baseline PINN Greeks (DELTA, GAMMA, VEGA, THETA, RHO).}
\end{figure}

\todos{MEJORAR ESTA FIGURA}


After adding Sobolev terms, Figure~\ref{fig:sobolev_greeks_error_maps} shows a clear contraction of high-error regions across all Greeks. The strongest visual improvement appears in GAMMA and VEGA, where problematic zones become much smaller and less intense than in the baseline maps. This spatial change is consistent with the order-of-magnitude RMSE reductions reported in Table~\ref{tab:ablation_metrics_by_greek}.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{figures/pinn/greeks/sobolev_rel_abs_error_maps_5greeks.png}
    \caption{Relative absolute-error maps for Sobolev-augmented PINN Greeks (DELTA, GAMMA, VEGA, THETA, RHO).}
    \label{fig:sobolev_greeks_error_maps}
\end{figure}

\todos{MEJORAR ESTA FIGURA}


The same effect appears in Figure~\ref{fig:sobolev_error_histograms}: Sobolev training narrows the distributions and reduces tail mass, so large deviations become less frequent for all five Greeks compared with the baseline histogram panel. The tail compression is particularly visible in GAMMA and VEGA, where baseline outliers were most persistent.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{figures/pinn/greeks/sobolev_error_hist_5greeks.png}
    \caption{Error histograms for Sobolev-augmented PINN Greeks (DELTA, GAMMA, VEGA, THETA, RHO).}
    \label{fig:sobolev_error_histograms}
\end{figure}

\todos{MEJORAR ESTA FIGURA}


Figure~\ref{fig:greeks_loss_sobolev} shows the two-phase optimization schedule used in this ablation: epochs \(0\) to \(4000\) are trained without Sobolev terms, and Sobolev augmentation is activated from epoch \(4000\) onward. Together, the five diagnostics indicate that Sobolev augmentation improves local derivative fidelity, not just aggregate scalar scores, while keeping the runtime overhead limited.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\textwidth]{figures/pinn/greeks/sobolev_loss_curve.png}
    \caption{Training loss trajectory with phase switch at epoch 4000 (without Sobolev before 4000, with Sobolev from 4000 onward).}
    \label{fig:greeks_loss_sobolev}
\end{figure}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Cross-Model Computational Comparison}
This section summarizes the end-to-end pipeline in operational order, emphasizing where each model contributes and how computational cost is distributed.

First, market calibration is driven by the ANN pricer plus the CaNN optimization loop. The ANN stage provides fast implied-volatility evaluations (\(\approx 8.43\times10^5\) points/s), while CaNN concentrates cost in iterative inverse fitting (\(134\) iterations, \(261\) objective evaluations) and reaches residual RMSE \(2.01441\times10^{-4}\) on the quote set.

Second, the calibrated market state \((\rho,\kappa,\gamma,\bar v,v_0)\) is injected into the parametric PINN pricing stage. In this configuration, baseline PINN pricing reaches RMSE \(5.62\times10^{-3}\) with \(R^2=0.99957\), and runs at \(\approx 4.91\times10^5\) points/s. Under the same benchmark protocol, COS runs at \(\approx 87.36\) points/s, so PINN remains several orders of magnitude faster in repeated evaluation mode.

Third, Greek extraction is performed on top of the same calibrated parameter state. The baseline PINN Greek pass runs at \(\approx 9.48\times10^3\) points/s, while the Sobolev variant runs at \(\approx 8.60\times10^3\) points/s (about \(9.3\%\) slower) and substantially improves derivative accuracy (for example, GAMMA RMSE from \(3.85\times10^{-1}\) to \(8.94\times10^{-3}\), VEGA RMSE from \(6.55\times10^{-2}\) to \(3.17\times10^{-3}\)).

Pipeline-level error should be read as stagewise bounds, not as a single scalar. The ANN and CaNN stages control IV-space mismatch used for calibration, pricing quality is validated in price space against COS, and Greek quality is validated in derivative space. Since these are different output spaces and objectives, the most reliable reading is the per-stage error envelope reported above and in Sections~6.2--6.5.
