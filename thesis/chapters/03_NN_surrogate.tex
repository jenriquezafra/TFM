\chapter{Supervised Neural Methods for Heston Pricing and Calibration}
\label{ch:ann_surrogate}
\label{ch:cann}

The main objective of this chapter is to construct the supervised neural block used in the thesis for Heston pricing, calibration, and sensitivity analysis. After the Black--Scholes implied-volatility convention (Section~\ref{sec:black_scholes_model}), the Heston dynamics (Section~\ref{sec:heston_model}), and the COS reference solver (Section~\ref{sec:fourier_cos_method}) have been fixed in Chapter~\ref{ch:background}, we now move from the numerical pricing layer to its supervised neural approximation. Within this block, we distinguish two final ANN pricers. The first one is the baseline ANN, which learns the Heston implied-volatility surface as a supervised regression problem and approximates the forward map
\[
(\Theta,m,\tau,r)\mapsto \sigma_{imp}.
\]
This model provides the reference supervised surrogate in IV space, using the notation and implied-volatility definition introduced in Section~\ref{sec:black_scholes_model} and computed through the inversion procedure of Section~\ref{sec:iv_numerical_inversion}.

The second model is the Sobolev ANN. This variant keeps the same input-output structure, but adds differential information during training. The goal is not to redefine the pricing problem, but to improve the local geometry of the learned IV surface. This matters because a surface with low pointwise IV error does not necessarily produce accurate sensitivities. The Sobolev extension addresses this limitation by adding derivative information computed from the Heston reference model.

Reference pricing based on the numerical methods introduced in Chapter~\ref{ch:background} is highly accurate but computationally expensive when repeated many times, as required in calibration and sensitivity workflows. In this project, the reference layer combines Heston-COS valuation (Section~\ref{sec:fourier_cos_method}) with implied-volatility inversion (Section~\ref{sec:iv_numerical_inversion}). The baseline ANN aims to reproduce this numerical pipeline efficiently, while the Sobolev ANN additionally uses derivative targets to make the surrogate more reliable for Greek estimation.

Finally, the chapter also introduces the neural calibration workflow. In this stage, the trained pricer is frozen and used as a fast evaluator inside an inverse problem that recovers Heston parameters from an observed implied-volatility surface. Methodologically, we follow the ANN-pricing and neural-calibration framework of Liu et al. \cite{liu} as the main reference, while documenting the specific implementation choices adopted in this project. The benchmark results are reported later in Chapter~\ref{ch:performance_benchmarks}; here the focus is the construction of the supervised pricing dataset, the pricer architecture, the baseline and Sobolev training protocols, and the calibration interface.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Problem Formulation}
\label{sec:ann_problem_formulation}

After the Heston dynamics and their COS pricing representation have been fixed in Sections~\ref{sec:heston_model} and~\ref{sec:fourier_cos_method}, we now pose vanilla option pricing under Heston as a supervised regression problem. Given a dataset
\[
\mathcal{D}=\{(\mathbf{x}_i,y_i)\}_{i=1}^{N},\qquad
y_i=\sigma_{imp,i},
\]
the neural network approximates the map
\[
f_{NN}:\mathbb{R}^{8}\to\mathbb{R}, \qquad
\mathbf{x}=(\rho,\kappa,\gamma,\bar v,v_0,m,\tau,r)\mapsto \sigma_{imp},
\]
where $m$ and $\tau$ follow the moneyness and time-to-maturity notation fixed in Chapter~\ref{ch:background}. Each label $\sigma_{imp}$ is obtained from Heston-consistent synthetic prices through numerical inversion.

The training objective is to estimate parameters $\mathbf{w}$ such that the empirical prediction error over $\mathcal{D}$ is minimized. In the pricing part of this chapter, the ANN family is used as a surrogate, i.e., as a fast approximation of the reference numerical pipeline in the forward map $(\Theta,m,\tau,r)\mapsto \sigma_{imp}$. In the calibration part, the same forward map is reused with the quote descriptors fixed and the Heston parameter vector varied.

This formulation defines the baseline ANN, which is trained only to minimize pointwise implied-volatility error. We also consider a Sobolev ANN with the same input-output map, but with additional targets for derivatives of the implied-volatility surface. The purpose of this second variant is not to change the pricing representation, but to improve the local geometry of the learned IV surface, which later makes the Greeks computed from the ANN implied volatility more stable.

We learn implied volatility instead of option price because the IV representation is directly comparable across contracts with different maturities and moneyness levels, and it is the market-standard quotation format for calibration and diagnostics. In contrast, raw prices mix scale effects (spot/strike/time) that make learning less homogeneous over the domain.

The resulting surrogates are primarily in-domain approximators. Preliminary out-of-domain checks indicate that the networks remain stable for moderate departures from the training bounds, while errors increase in extreme regimes. For this reason, the methodological core of this chapter is developed on the predefined training domain, and the extrapolation behaviour is discussed separately in the generalization and robustness analysis.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Synthetic Dataset And Supervised Learning Vocabulary}
\label{sec:ann_synthetic_dataset}
Training these supervised surrogates requires a large set of labeled input-output pairs. To obtain a controlled and reproducible source, we generate synthetic data under the Heston model introduced in Section~\ref{sec:heston_model}. The pricing and implied-volatility conventions come from Chapter~\ref{ch:background}; this section fixes the dataset vocabulary used by the implementation.

In the current pipeline, generation is organized as a \emph{master dataset}: a table whose rows are available to several downstream workflows. Each row stores the variables needed for supervised learning and numerical diagnostics:
\begin{itemize}
    \item The \emph{features} are the neural-network inputs $(\rho,\kappa,\gamma,\bar v,v_0,m,\tau,r)$.
    \item The \emph{training label} is the implied volatility $\sigma_{\mathrm{imp}}$, stored as \texttt{sigma\_imp\_brent}, obtained from the reference numerical pipeline.
    \item The \emph{auxiliary target} is the COS price \texttt{price\_cos}, retained as a reference quantity for diagnostics.
    \item The \emph{quality flags} are metadata used to assess numerical reliability. They record the Feller diagnostic and the Brent-inversion status and residual used for filtering and robustness checks.
\end{itemize}
The terms \emph{split} and \emph{preprocessing} refer, respectively, to the train/validation/test partition and to the transformations applied before training, such as row filtering, precision changes, or feature/target normalization. This design allows ANN and PINN workflows to reuse the same synthetic source while applying experiment-specific filters only at training or evaluation time.

The raw variable set contains the Heston parameters from Equation~\eqref{eq:heston_dynamics} and the contract/market variables $(K,S_0,\tau,r)$. The background chapter uses $\bar\nu$ and $\nu_0$ for variance quantities; the dataset columns $\bar v$ and $v_0$ denote the same long-run and initial variance levels in implementation notation. We fix $K=1$ and keep the moneyness coordinate \(m\) as the relative underlying level. This is consistent with the log-moneyness state used in the Heston-COS formula in Equation~\eqref{eq:heston_log_moneyness_cf}, since $x=\log(S_0/K)=\log m$. The resulting ANN input vector has eight features:
\[
\mathbf{x}=(\rho,\kappa,\gamma,\bar v,v_0,m,\tau,r)\in\mathbb{R}^8.
\]

A full Cartesian grid is computationally prohibitive in this dimension, since using $N$ points per variable scales as $N^8$ (already intractable for moderate $N$). Therefore, the synthetic inputs are sampled with Latin Hypercube Sampling (LHS), which stratifies each coordinate and provides broad marginal coverage for a prescribed number of samples \cite{mckay_2000}. In our implementation, LHS is applied directly to the full input vector with a fixed seed and a target size of $10^7$ samples in the master dataset. The specific ranges, constraints, and splitting/preprocessing rules are detailed below.

\subsection{Parameter Ranges And Constraints}
The sampling domain combines Heston parameters, contract variables, and market rate. Following the ranges used in our configuration (largely aligned with \cite{liu}), the training domain is summarized in Table~\ref{tab:ann_synth_input_ranges}.

\begin{table}[H]
    \ttabbox[\FBwidth]
    {\caption{Input ranges used for synthetic data generation}\label{tab:ann_synth_input_ranges}}
    {\begin{tabular}{|P{4.0cm}|P{7.8cm}|}
        \hline
        \textbf{Parameter} & \textbf{Range} \\
        \hline
        $\rho$ & $[-0.9,\,0.0]$ \\
        \hline
        $\kappa$ & $[0.0,\,3.0]$ \\
        \hline
        $\gamma$ & $[0.01,\,0.8]$ \\
        \hline
        $\bar v$ & $[0.01,\,0.5]$ \\
        \hline
        $v_0$ & $[0.05,\,0.5]$ \\
        \hline
        $m$ & $[0.6,\,1.4]$ \\
        \hline
        $\tau$ & $[0.05,\,3.0]$ \\
        \hline
        $r$ & $[0.0,\,0.05]$ \\
        \hline
        \multicolumn{2}{l}{Source: Based on \cite{liu} and project configuration choices}
    \end{tabular}}
\end{table}

The maturity range enforces $\tau>0$, which is required by the pricing and implied-volatility conventions introduced in Chapter~\ref{ch:background}. We also record the Feller diagnostic from Equation~\eqref{eq:feller_condition}. In the dataset, \texttt{feller\_slack} stores the signed margin of that condition and \texttt{feller\_ok} stores the corresponding Boolean pass/fail flag. Feller-violating points are \emph{not} removed during generation; they are kept in the master dataset for controlled ablation studies and can be filtered later if a particular experiment requires it.

Recalling that the strike is fixed at \(K=1\), the quote grid is expressed through \(m\). This reduces the dimensionality from nine variables $(\Theta,S_0,K,\tau,r)$ to eight inputs $(\Theta,m,\tau,r)$ without loss of information for this setup.

\subsection{Dataset Splitting And Preprocessing}
After generation, we keep all selected rows in the master dataset and split into train/validation/test subsets with proportions $0.8/0.1/0.1$. In the reported configuration, splits are random (without mandatory stratification), and filtering policies are deferred to each training experiment.

In the baseline configuration, no explicit feature or target normalization is applied (Liu-style setup). Therefore, the model is trained directly on the physical scale of the variables $(\rho,\kappa,\gamma,\bar v,v_0,m,\tau,r)$ and predicts IV in absolute units.

Data quality control is integrated in the synthesis pipeline through explicit flags, not hard row deletion by default. In particular, we track Brent inversion status and the absolute price reconstruction residual associated with the IV inversion of Section~\ref{sec:iv_numerical_inversion}. Keeping these quantities in the table makes the filtering rule auditable: a later experiment can state exactly whether it used all rows, only successful IV inversions, only Feller-admissible rows, or a stricter residual threshold. For storage efficiency, output tables are persisted in \texttt{float32}, whereas COS pricing and Brent inversion are computed internally in \texttt{float64}.

\subsection{Numerical Label Generation (COS + Brent)}
Each label is produced in two deterministic numerical steps already defined in the background chapter. First, the target option value is computed with the Heston-COS solver for the sampled feature vector. Second, that value is converted to implied volatility using the Brent inversion routine from Section~\ref{sec:iv_numerical_inversion}. Thus, the ANN target is a Heston-consistent implied volatility, not an independently assumed Black--Scholes volatility.

In our implementation, Brent is configured with strict numerical controls (absolute tolerance $10^{-8}$, bounded search interval, and iteration cap). Additionally, when the inferred volatility hits the lower IV floor, an adaptive COS fallback can be triggered by widening the truncation interval before retrying inversion. This is the same numerical-error concern discussed in Appendix~\ref{app:cos_error}: COS labels must be stable because they are later treated as supervised targets.

Rather than dropping those edge cases at generation time, the pipeline records quality metadata (\texttt{brent\_success}, \texttt{brent\_abs\_residual}, and retry diagnostics) so that robustness filters can be applied transparently at training time. This preserves full traceability of numerical reliability across the domain.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Neural Network Architecture}
Both ANN pricers are implemented as fully connected neural networks that approximate a smooth nonlinear map from model/contract inputs to implied volatility. This choice is consistent with the structure of the problem: inputs are low-dimensional continuous features without spatial topology or sequential dependence, and the target is a scalar regression value.

From a function-approximation perspective, a feed-forward multilayer perceptron (MLP) provides enough expressive capacity to capture the interactions between Heston parameters, moneyness, maturity, and rates on the selected domain. Compared with architectures designed for images or sequences (e.g., CNNs or transformers), an MLP is more parsimonious and easier to train for this tabular setting.

This choice is also consistent with the classical universal-approximation results: under standard assumptions, feed-forward networks with non-polynomial activations can approximate continuous functions on compact sets to arbitrary accuracy \cite{cybenko1989,hornik1991}. In this thesis, this theorem is used as a representation-capacity motivation, while practical accuracy is established empirically through out-of-sample benchmarks.

\subsection{Model Class}
Following \cite{liu}, we use a fully connected feed-forward network (FCNN). Let $\mathbf{x}\in\mathbb{R}^8$ be
\[
\mathbf{x}=(\rho,\kappa,\gamma,\bar v,v_0,m,\tau,r),
\]
and let $y=\sigma_{imp}\in\mathbb{R}$ denote the target implied volatility. The model defines a parametric function
\[
\hat \sigma_{imp} = f_{NN}(\mathbf{x};\mathbf{w}),
\]
with trainable parameters $\mathbf{w}$.

Once $\mathbf{w}$ is fixed after training, inference is deterministic: for the same input $\mathbf{x}$, the network always returns the same prediction $\hat \sigma_{imp}$. This property is important for downstream calibration and sensitivity analysis, where numerical consistency across repeated calls is required.

The FCNN acts as a surrogate only at inference time. Target labels are still generated with the numerical pipeline (Heston COS pricing followed by implied-volatility inversion), so the network learns to emulate that reference solver on the sampled domain.

\subsection{Architecture Details}
The selected architecture used in the final pipeline is
\[
8 \rightarrow 200 \rightarrow 200 \rightarrow 200 \rightarrow 200 \rightarrow 1,
\]
with \texttt{tanh} activation in hidden layers and linear activation in the output layer. In compact form,
\[
h^{(1)}=\phi(W^{(1)}x+b^{(1)}),\quad \dots,\quad
h^{(4)}=\phi(W^{(4)}h^{(3)}+b^{(4)}),\quad
\hat y=W^{(5)}h^{(4)}+b^{(5)},
\]
where $\phi(\cdot)=\tanh(\cdot)$.

Weights are initialized with Xavier uniform initialization and biases are initialized to zero. In the baseline setup, dropout is disabled ($p=0$) and no batch normalization layers are used. This configuration follows a controlled, reproducible design intended to isolate the impact of optimization and data quality choices.

\begin{table}[H]
    \ttabbox[\FBwidth]
    {\caption{Neural network architecture used for the ANN pricer}\label{tab:ann_architecture}}
    {\begin{tabular}{|P{5.2cm}|P{6.6cm}|}
        \hline
        \textbf{Hyperparameter} & \textbf{Value} \\
        \hline
        Input dimension & 8 \\
        \hline
        Hidden layers & 4 \\
        \hline
        Hidden width & 200 neurons per layer \\
        \hline
        Hidden activation & \texttt{tanh} \\
        \hline
        Output dimension & 1  \\
        \hline
        Output activation & Linear \\
        \hline
        Weight initialization & Xavier uniform \\
        \hline
        Bias initialization & Zeros \\
        \hline
        Dropout & $p=0.0$ \\
        \hline
        Batch normalization & Disabled \\
        \hline
        \multicolumn{2}{l}{Source: Based on \cite{liu}, with selected \texttt{tanh} activation}
    \end{tabular}}
\end{table}

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        >=Latex,
        neuron/.style={circle,draw=black,minimum size=4.2mm,inner sep=0pt,line width=0.35pt},
        input neuron/.style={neuron,fill=black},
        trainable neuron/.style={neuron,fill=white},
        connection/.style={->,line width=0.22pt,shorten >=1.0pt,shorten <=1.0pt},
        layer label/.style={font=\large\bfseries},
        legend text/.style={font=\small},
        transform shape
    ]
        \draw[rounded corners=4pt,line width=0.35pt] (-1.30,-3.05) rectangle (13.05,2.75);

        \node[layer label] at (0,2.35) {Input layer};
        \node[layer label] at (6.10,2.35) {Hidden layers};
        \node[layer label] at (11.65,2.35) {Output layer};

        \node[input neuron,label=left:{\small$\rho$}] (I1) at (0,1.75) {};
        \node[input neuron,label=left:{\small$\kappa$}] (I2) at (0,1.25) {};
        \node[input neuron,label=left:{\small$\gamma$}] (I3) at (0,0.75) {};
        \node[input neuron,label=left:{\small$\bar v$}] (I4) at (0,0.25) {};
        \node[input neuron,label=left:{\small$v_0$}] (I5) at (0,-0.25) {};
        \node[input neuron,label=left:{\small$m$}] (I6) at (0,-0.75) {};
        \node[input neuron,label=left:{\small$\tau$}] (I7) at (0,-1.25) {};
        \node[input neuron,label=left:{\small$r$}] (I8) at (0,-1.75) {};

        \foreach \layer/\xpos in {1/2.45,2/4.90,3/7.35,4/9.80} {
            \foreach \nodeidx/\ypos in {1/1.40,2/0.50,3/-0.50,4/-1.40} {
                \node[trainable neuron] (H\layer\nodeidx) at (\xpos,\ypos) {};
            }
            \node[font=\normalsize] at (\xpos,0) {$\vdots$};
        }
        \node[trainable neuron] (O1) at (11.65,0) {};
        \node[font=\small,anchor=west] at (11.95,0) {$\hat\sigma_{\mathrm{imp}}$};

        \foreach \i in {1,...,8} {
            \foreach \j in {1,...,4} {
                \draw[connection] (I\i) -- (H1\j);
            }
        }
        \foreach \layer/\next in {1/2,2/3,3/4} {
            \foreach \i in {1,...,4} {
                \foreach \j in {1,...,4} {
                    \draw[connection] (H\layer\i) -- (H\next\j);
                }
            }
        }
        \foreach \i in {1,...,4} {
            \draw[connection] (H4\i) -- (O1);
        }

        \node[input neuron] at (2.45,-2.35) {};
        \node[legend text,anchor=west] at (2.75,-2.35) {Fixed input feature};
        \node[trainable neuron] at (6.65,-2.35) {};
        \node[legend text,anchor=west] at (6.95,-2.35) {Trainable affine layer};
    \end{tikzpicture}
    \caption{Fully connected ANN pricer used in the forward Heston implied-volatility surrogate. The eight fixed inputs are mapped through four \texttt{tanh} hidden layers of width \(200\), and the final linear layer returns the predicted implied volatility.}
    \label{fig:ann_architecture_tikz}
\end{figure}




\subsection{Output Interpretation}
The network output is a scalar implied volatility in annualized units (decimal form), not an option price. Therefore, prediction errors are interpreted directly in IV space, which is the representation used in the calibration workflow and in most volatility-surface diagnostics.

Given a network prediction
\[
\hat\sigma_{\mathrm{imp}} = f_{NN}(\rho,\kappa,\gamma,\bar v,v_0,m,\tau,r),
\]
the price implied by the ANN output is obtained as
\[
\hat V_{\mathrm{ANN}} =
V_{BS}(m,\tau,r,\hat\sigma_{\mathrm{imp}}).
\]
Greeks are then computed by differentiating this composed map with respect to the relevant input variable \(q\):
\begin{equation}
\label{eq:ann_greek_composed_map}
\mathrm{Greek}_{\mathrm{ANN}}(q)
=
\frac{\partial}{\partial q}
V_{BS}\!\left(m,\tau,r,f_{NN}(\rho,\kappa,\gamma,\bar v,v_0,m,\tau,r)\right).
\end{equation}
Thus, Greek accuracy depends not only on the pointwise IV error, but also on the smoothness and derivative quality of the learned IV surface.

Because the output layer is linear, the model is not hard-constrained to produce strictly positive values for every possible input. In practice, training on a physically admissible domain with positive IV labels strongly reduces this risk inside the sampled region. For downstream use, any out-of-domain prediction can be explicitly checked and, if required, filtered before subsequent numerical steps.

This output definition also preserves differentiability of the surrogate with respect to inputs, which is useful in later chapters when the ANN pricer is embedded in calibration and sensitivity pipelines.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Training Procedure}
Training the network means adjusting the weights \(\mathbf{w}\) so that the empirical loss on the supervised dataset decreases. At this stage we describe the common training loop; the numerical rule used to update the weights, and the meaning of optimizer in this setting, are introduced in Section~\ref{sec:ann_optimizer_hyperparameters}.

The training loop is fully configuration-driven and follows an epoch-based supervised learning scheme. At each epoch, the configured update rule modifies the parameters using the training split, then the model is evaluated on the validation split without gradient computation. This separates the learning step from the monitoring step: the former changes \(\mathbf{w}\), while the latter only measures out-of-sample behaviour under the current weights.

Each epoch stores train/validation metrics, current update-rule identity, and learning rate. When enabled, the validation monitor can be either the raw validation loss or a trimmed variant that discards the top-tail hardest samples before aggregation. This provides a robust alternative when a small fraction of outlier points may dominate the monitor value.

Execution device is selected automatically (\texttt{mps} when available, otherwise \texttt{cpu}), and data-shuffling reproducibility is controlled through a fixed generator seed from configuration. During training, two checkpoints are maintained: the \textit{last} checkpoint (always overwritten each epoch) and the \textit{best} checkpoint (updated only when the monitored validation metric improves). The best checkpoint is used as the reference model for post-training evaluation and downstream calibration.

\subsection{Baseline ANN Loss Function}
Let $\{(\mathbf{x}_i,\sigma_i)\}_{i=1}^{B}$ denote a mini-batch, where $\sigma_i$ is the target implied volatility and $\hat{\sigma}_i=f_{NN}(\mathbf{x}_i;\mathbf{w})$. The baseline objective is mean squared error (MSE):
\begin{equation}
\label{eq:ann_baseline_mse_loss}
\mathcal{L}_{\text{MSE}}(\mathbf{w})=\frac{1}{B}\sum_{i=1}^{B}\left(\hat{\sigma}_i-\sigma_i\right)^2.
\end{equation}

Here \(B\) is the mini-batch size, \(\sigma_i\) is the reference implied volatility obtained from the Heston-COS/Brent pipeline, and \(\hat{\sigma}_i\) is the ANN prediction for the same input \(\mathbf{x}_i\).

MSE is preferred because it is smooth, differentiable everywhere, and strongly penalizes large deviations, which is desirable when the ANN is later used in calibration loops sensitive to local pricing errors. RMSE and MAPE are then used as reporting metrics, following the convention fixed in Chapter~\ref{ch:performance_benchmarks}.

\subsection{Sobolev ANN Loss Function}
For the Sobolev ANN, the MSE term is kept as the value-matching component, but the loss is augmented with derivative information from a separate anchor set. Let
\[
\mathcal{J}=\{m,\tau,r,v_0\}
\]
be the input directions used for Sobolev supervision, and let \(d_{i,q}\) denote the target derivative \(\partial \sigma_{\mathrm{imp}}/\partial q\) at anchor point \(\mathbf{x}_i\). The derivative loss is
\begin{equation}
\label{eq:ann_sobolev_derivative_loss}
\mathcal{L}_{\mathrm{Sob}}(\mathbf{w})
=
\frac{1}{B_{\mathrm{Sob}}|\mathcal{J}|}
\sum_{i=1}^{B_{\mathrm{Sob}}}
\sum_{q\in\mathcal{J}}
\left(
\frac{\partial_q f_{NN}(\mathbf{x}_i;\mathbf{w})-d_{i,q}}{s_q}
\right)^2.
\end{equation}
Here \(B_{\mathrm{Sob}}\) is the Sobolev mini-batch size, \(q\) indexes the supervised derivative directions, and \(d_{i,q}\) is the Heston-consistent IV derivative target. The factor \(s_q\) is a robust scale computed from the reference IV derivatives in direction \(q\). It normalizes the derivative errors so that directions with larger raw magnitudes do not dominate the loss. This follows the Sobolev-training idea of matching both function values and derivatives \cite{czarnecki2017sobolev}, adapted here to the implied-volatility surface. The total Sobolev objective is then
\begin{equation}
\label{eq:ann_sobolev_total_loss}
\mathcal{L}_{\mathrm{ANN\text{-}Sob}}(\mathbf{w})
=
\mathcal{L}_{\mathrm{MSE}}(\mathbf{w})
+
\lambda_{\mathrm{Sob}}\mathcal{L}_{\mathrm{Sob}}(\mathbf{w}).
\end{equation}
Here \(\mathcal{L}_{\mathrm{MSE}}\) is the baseline value loss from Equation~\eqref{eq:ann_baseline_mse_loss}. In the retained Sobolev ANN run, \(\lambda_{\mathrm{Sob}}=0.10\), the Sobolev batch size is \(512\), and the derivative term is activated after a warm-up of \(500\) epochs. 

\subsection{Optimizer And Hyperparameters}
\label{sec:ann_optimizer_hyperparameters}
In this context, an optimizer is the numerical rule that updates the network weights \(\mathbf{w}\) in order to reduce the loss. In a full-gradient method, the update direction would be computed from all training samples at each step. For a dataset of this size, this is unnecessarily expensive, so the practical alternative is to estimate the gradient from mini-batches. This gives a stochastic gradient: it is cheaper to compute, but it introduces sampling noise because each batch only approximates the full empirical gradient.

Adam belongs to the family of stochastic gradient methods, but it modifies plain stochastic gradient descent by using adaptive moments. More precisely, it keeps exponential moving averages of the mini-batch gradient and of its squared values, and uses them to scale the update of each coordinate. The learning rate fixes the base step size before this adaptive rescaling, while the optimizer weight decay controls whether the update also includes an \(L^2\)-type shrinkage of the weights. The values used in the retained ANN configurations are reported in Table~\ref{tab:ann_selected_hyperparameters}: the Adam learning rate starts at \(10^{-3}\), and weight decay is set to \(0.0\), so no additional regularization is imposed through Adam itself. This is useful in our supervised ANN setting because the dataset is large, the loss is non-convex, and the different input directions may produce gradients with different magnitudes.

L-BFGS follows a different logic. It is a limited-memory method that uses recent parameter and gradient differences to build a compact approximation of the local curvature of the loss \cite[Ch.~7]{nocedal2006numerical}. In practical terms, it tries to use more information than the current gradient alone, which can help when the parameters are already close to a good region. The trade-off is computational: each L-BFGS step is heavier and requires reevaluating the loss and its gradient through a closure. For this reason, in this project L-BFGS is mainly tested as a refinement stage after an Adam phase, not as the default training rule for the full ANN run.

The optimizer experiments followed this logic. First, Adam-only runs were used to establish stable baselines under the Liu-style architecture, varying activation, learning-rate schedule, weight decay, and early-stopping policy. Then, mixed schedules were tested by switching from Adam to L-BFGS after an initial stochastic phase. The motivation for these mixed runs was reasonable: Adam can move efficiently through the large dataset, while L-BFGS might improve the final local fit. The retained mixed candidate did not improve the held-out error profile of the selected Adam--\texttt{tanh} run, so L-BFGS remains part of the experimental path but not part of the final reference surrogate. The quantitative comparison is reported with the ANN benchmark results in Table~\ref{tab:ann_optimizer_selection}.

Under this criterion, the selected baseline ANN is the Liu-style architecture trained with Adam and \texttt{tanh} hidden activations. It preserves the reference width and depth, improves on the earlier ReLU baseline in the retained benchmark comparison, and provides the baseline forward evaluator used in the CaNN comparison. The Sobolev ANN keeps this same architecture and optimizer backbone, but replaces the objective by Equation~\eqref{eq:ann_sobolev_total_loss}.

\subsection{Regularization And Callbacks}
Regularization in the selected ANN configurations is intentionally light: dropout is disabled ($p=0$), batch normalization is disabled, and Adam weight decay is set to zero. In addition, feature/target normalization is turned off in the Liu-style reference setup. This design keeps the training protocol close to the reference architecture and isolates the effect of the selected hidden activation, optimizer scheduling, and data quality controls.

A learning-rate scheduler is a callback that changes the optimizer step size during training according to a prescribed rule. In the selected ANN configurations we use StepLR, which reduces Adam's learning rate by a factor \(\gamma\) after a fixed number of epochs. Early stopping follows a different purpose: it monitors validation performance and interrupts training when the selected validation criterion stops improving over a given patience window. In the retained runs this mechanism is available in the training pipeline but disabled, so model selection relies on the stored \textit{best} and \textit{last} checkpoints rather than on an automatic stopping rule.

Callback management therefore includes: (i) checkpointing of both \textit{best} and \textit{last} models, (ii) StepLR scheduling for Adam, and (iii) an early-stopping module available in the pipeline but disabled in the baseline experiments. When enabled, early stopping supports both standard validation loss and trimmed validation loss monitors.

From an experimental standpoint, this callback configuration reduces the risk of selecting a late degraded checkpoint without over-constraining the optimization path. The scheduler controls the Adam step size in the selected ANN configurations, while the Adam-to-L-BFGS transition remains available for mixed experiments. Generalization is then assessed empirically on held-out validation and test splits rather than enforced through strong explicit regularizers.

\begin{table}[H]
    \ttabbox[\FBwidth]
    {\caption{Hyperparameters and training controls of the selected ANN configurations}\label{tab:ann_selected_hyperparameters}}
    {\begin{tabular}{|P{5.0cm}|P{6.8cm}|}
        \hline
        \textbf{Hyperparameter} & \textbf{Value} \\
        \hline
        Training mode & Adam \\
        \hline
        Hidden activation & \texttt{tanh} \\
        \hline
        Seed & 42 \\
        \hline
        Epochs & 8000 \\
        \hline
        Train batch size & 1024 \\
        \hline
        Validation batch size & All validation samples \\
        \hline
        Baseline loss & MSE, Equation~\eqref{eq:ann_baseline_mse_loss} \\
        \hline
        Sobolev extension & \(\lambda_{\mathrm{Sob}}=0.10\), batch \(512\), warm-up \(500\) epochs \\
        \hline
        Adam learning rate & \(10^{-3}\) \\
        \hline
        Adam weight decay & 0.0 \\
        \hline
        LR scheduler & StepLR, step size \(=500\), \(\gamma=0.5\) \\
        \hline
        Early stopping & Disabled \\
        \hline
        \multicolumn{2}{l}{Source: Own elaboration from run configuration copies}
    \end{tabular}}
\end{table}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Neural Calibration As An Inverse Problem}
\label{sec:cann_inverse_problem}

The supervised ANN pricers constructed in the previous sections approximate the forward Heston map from parameters and contract descriptors to implied volatility. Neural calibration uses the same idea in the opposite direction: given a finite implied-volatility surface, infer the Heston parameter vector
\[
\theta=(\rho,\kappa,\gamma,\bar v,v_0).
\]
Here the parameter meanings are those fixed in Table~\ref{tab:heston_parameters}, with the same dataset notation convention for $\bar v$ and $v_0$ described in Section~\ref{sec:ann_synthetic_dataset}. The calibration workflow follows the neural-calibration idea of Liu et al. \cite{liu}: a trained ANN pricer is frozen and used as a fast evaluator inside a calibration loop.

The purpose is not to introduce a new calibration algorithm. The contribution in this thesis is the integration of this calibration step into the ANN--PINN workflow and the diagnostic reading of its output. For that reason, this section keeps only the setup needed to interpret the benchmark in Chapter~\ref{ch:performance_benchmarks}.

\subsection{Calibration Setup}
\label{sec:cann_setup}

Each trained ANN surrogate approximates the forward map
\[
f_{NN}:(\rho,\kappa,\gamma,\bar v,v_0,m,\tau,r)\mapsto \sigma_{\mathrm{IV}},
\]
where \(m\), \(\tau\), \(r\), and \(\sigma_{\mathrm{IV}}\) keep the notation fixed in Chapter~\ref{ch:background}. In calibration, the quote descriptors are fixed and only \(\theta\) is varied. For each candidate \(\theta\), the network is evaluated on the quote set
\[
\mathcal{Q}=\{(m_j,\tau_j,r_j,\sigma^{\mathrm{mkt}}_j,w_j)\}_{j=1}^{N_q},
\]
where \(w_j\geq0\) is an optional quote weight. The selected benchmark uses \(N_q=35\) quotes on a complete \(5\times7\) moneyness--maturity grid with constant \(r=0.03\); the exact nodes are reported in Section~\ref{sec:cann_performance}.

For quote \(j\), the predicted implied volatility and residual are
\[
\hat\sigma_j(\theta)=f_{NN}(\rho,\kappa,\gamma,\bar v,v_0,m_j,\tau_j,r_j),
\qquad
e_j(\theta)=\hat\sigma_j(\theta)-\sigma^{\mathrm{mkt}}_j.
\]
Calibration is therefore performed in implied-volatility space, matching the ANN training target and the market convention used for volatility smiles.

The objective minimized in the calibration loop is
\begin{equation}
\label{eq:cann_objective}
J(\theta)
=
\frac{1}{\sum_{j=1}^{N_q} w_j}
\sum_{j=1}^{N_q} w_j e_j(\theta)^2
+\lambda R(\theta),
\end{equation}
where \(R(\theta)\) is an optional regularization penalty. The calibrated vector is
\begin{equation}
\label{eq:cann_argmin}
\theta^\star
=
\arg\min_{\theta\in\mathcal{B}}J(\theta),
\end{equation}
with \(\mathcal{B}\) the admissible Heston parameter box. In this thesis, the bounds are aligned with the ANN training domain: \(\rho\in[-0.9,0]\), \(\kappa\in[0,3]\), \(\gamma\in[0.01,0.8]\), \(\bar v\in[0.01,0.5]\), and \(v_0\in[0.05,0.5]\). This restriction keeps calibration inside the intended interpolation regime of the forward surrogate.

\subsection{Search And Stability Checks}
\label{sec:cann_search_stability}

The inverse residual surface is not assumed to be convex, so the reference calibration uses Differential Evolution followed by local polishing. This is a pragmatic choice: the method is robust to initialization, does not require gradients of the neural objective, and can evaluate a population of candidates through batched ANN inference.

The calibration protocol is the same for both CaNN variants considered in the benchmark. The baseline CaNN uses the frozen ANN baseline surrogate, while the Sobolev CaNN uses the frozen ANN Sobolev surrogate. In both cases, the quote grid, parameter bounds, objective \(J(\theta)\), and Differential Evolution plus polishing strategy are kept fixed; only the forward implied-volatility evaluator \(f_{NN}\) is replaced. Thus, Sobolev training changes the quality of the surrogate used inside calibration, but it does not define a different calibration objective.

The calibration configuration is kept inside the interpolation domain of the ANN surrogates. It uses the 35-quote grid described above, no regularization in the reference objective (\(\lambda=0\)), and the same parameter bounds as the ANN training domain. Differential Evolution is run with maximum iteration count \(1000\), population multiplier \(20\), tolerance \(10^{-4}\), mutation range \((0.5,1.0)\), recombination \(0.7\), and final polishing enabled.

The selected solution is assessed at two levels. First, quote-level residuals check whether the implied-volatility surface is reproduced. Second, parameter diagnostics check whether the selected optimum is locally stable. Around a low-residual optimum, the residual data term can be read through the Gauss--Newton approximation
\[
H(\theta^\star)\approx 2J_e(\theta^\star)^\top WJ_e(\theta^\star),
\]
where \(J_e(\theta^\star)\) is the Jacobian of quote residuals with respect to \(\theta\), and \(W\) is the diagonal matrix of quote weights. Large curvature indicates directions where the quote surface reacts strongly to parameter changes; small curvature indicates flatter, less identified directions.

These diagnostics are not interpreted here as results; they define the checks used later to decide whether a calibration is both low-residual and locally stable. The numerical recovery, residual metrics, and parameter-error tables are reported in Chapter~\ref{ch:performance_benchmarks}.
